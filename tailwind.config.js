/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class',
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // Include all relevant file types in your `src` directory
  ],
  theme: {
    extend: {}, // Customize the default theme here if needed
  },
  plugins: [],
};
